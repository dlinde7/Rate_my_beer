function onClick(){
    console.log("nothing");
    window.location.href = "../register";
};

// SELECT idUser FROM ratemybeer.Users
// WHERE Username = {user.username} AND Password = {user.password};